[workspace]
resolver = "2"
members = ["crates/selene-shell"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
authors = ["leriart"]
description = "Selene -- a QML shell for Hyprland, written in Rust"
publish = false

[profile.release]
opt-level = 3
lto = "thin"
